var Browser = require('zombie');
var assert = require('chai').assert;
var browser;

Browser.localhost(

suite('Cross-Page Test', function() {
    setup(function() {
        browser = new Browser
    });

    test('requestina group rate quote', function() {
        
    });
});
